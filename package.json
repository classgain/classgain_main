{
  "name": "what-next-project",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix seller && npm install --prefix admin && npm install --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:clients": "npm run dev --prefix client",
    "dev:seller": "npm run dev --prefix seller",
    "dev:admin": "npm run dev --prefix admin",
    "dev:admin:full": "node scripts/dev-admin.js",
    "dev:server": "npm run dev --prefix server",
    "db:setup": "npm run db:setup --prefix server",
    "build": "npm run build --prefix client",
    "build:clients": "npm run build --prefix client",
    "build:seller": "npm run build --prefix seller",
    "build:admin": "npm run build --prefix admin",
    "build:all": "npm run build:clients && npm run build:seller && npm run build:admin",
    "lint": "eslint admin/src client/src seller/src server/src scripts",
    "start:server": "npm start --prefix server"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.7.0"
  }
}
